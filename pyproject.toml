[project]
name = "bitcoin-fee-benchmarks"
version = "0.1.0"
description = "Benchmark suite for Bitcoin fee prediction algorithms"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}

[project.optional-dependencies]
dev = [
    "pytest",
]
